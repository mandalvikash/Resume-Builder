# MongoDB connection string (required for backend)
MONGODB_URI=mongodb+srv://portfolio:Vikash%401306@cluster0.cku1n43.mongodb.net/genc-dossier

# JWT secret for login (change in production)
JWT_SECRET=your-secret-key-change-in-production

# Optional: backend port (default 5000)
PORT=5000
